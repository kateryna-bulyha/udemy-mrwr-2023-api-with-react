function SearchBar ({ onSubmit }) {
    const handleClick = () => {
        onSubmit('forest')
    };


    return <div>
        <input />
        <button>Click me!</button>
    </div>
}

export default SearchBar;
